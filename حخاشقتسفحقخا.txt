<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Venor Security</title>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: Tahoma, Arial, sans-serif;
    }

    body {
      min-height: 100vh;
      background: radial-gradient(circle at center, #101010 0%, #050505 55%, #000 100%);
      color: #00ff66;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
      padding: 18px;
    }

    .grid {
      position: fixed;
      inset: 0;
      background-image:
        linear-gradient(rgba(0, 255, 102, 0.09) 1px, transparent 1px),
        linear-gradient(90deg, rgba(0, 255, 102, 0.09) 1px, transparent 1px);
      background-size: 35px 35px;
      animation: move 5s linear infinite;
      opacity: 0.25;
    }

    @keyframes move {
      from { background-position: 0 0; }
      to { background-position: 0 70px; }
    }

    .box {
      position: relative;
      z-index: 2;
      width: min(94vw, 780px);
      background: rgba(0, 0, 0, 0.88);
      border: 2px solid #00ff66;
      border-radius: 24px;
      padding: 28px 22px;
      text-align: center;
      box-shadow: 0 0 35px rgba(0,255,102,0.35);
    }

    .danger {
      color: #ff3333;
      font-size: clamp(36px, 7vw, 64px);
      margin-bottom: 10px;
      text-shadow: 0 0 18px rgba(255, 0, 0, 0.8);
      animation: blink 0.7s infinite alternate;
    }

    @keyframes blink {
      from { opacity: 0.65; transform: scale(1); }
      to { opacity: 1; transform: scale(1.05); }
    }

    h1 {
      font-size: clamp(28px, 6vw, 54px);
      color: #ff3333;
      text-shadow: 0 0 15px rgba(255,0,0,0.75);
      margin-bottom: 12px;
    }

    .sub {
      color: #eee;
      font-size: clamp(16px, 3vw, 22px);
      margin-bottom: 20px;
    }

    .terminal {
      direction: ltr;
      text-align: left;
      background: #030303;
      border: 1px solid rgba(0,255,102,0.55);
      border-radius: 16px;
      padding: 18px;
      min-height: 230px;
      color: #00ff66;
      font-family: Consolas, monospace;
      line-height: 1.8;
      overflow: hidden;
      box-shadow: inset 0 0 18px rgba(0,255,102,0.12);
    }

    .line::before {
      content: "> ";
      color: #fff;
    }

    .progress-wrap {
      margin-top: 20px;
      height: 18px;
      border: 1px solid #00ff66;
      background: #111;
      border-radius: 999px;
      overflow: hidden;
    }

    .progress {
      height: 100%;
      width: 0%;
      background: linear-gradient(90deg, #00ff66, #d7ff00);
      transition: width 0.25s;
    }

    .percent {
      margin-top: 10px;
      font-size: 20px;
      color: #d7ff00;
      font-weight: bold;
    }

    .reveal {
      display: none;
      margin-top: 20px;
      background: rgba(255,255,255,0.09);
      color: #fff;
      border-radius: 18px;
      padding: 18px;
      font-size: clamp(18px, 4vw, 30px);
      line-height: 1.7;
      border: 1px solid rgba(255,255,255,0.15);
    }

    .safe {
      color: #00ff66;
      font-weight: bold;
    }

    .shake {
      animation: shake 0.12s infinite;
    }

    @keyframes shake {
      0% { transform: translate(0, 0); }
      25% { transform: translate(2px, -2px); }
      50% { transform: translate(-2px, 2px); }
      75% { transform: translate(2px, 2px); }
      100% { transform: translate(-2px, -2px); }
    }

    button {
      margin-top: 18px;
      padding: 13px 24px;
      border: 0;
      border-radius: 999px;
      background: #00ff66;
      color: #000;
      font-weight: bold;
      font-size: 17px;
      cursor: pointer;
      box-shadow: 0 0 20px rgba(0,255,102,0.45);
    }

    button:hover {
      background: #d7ff00;
      transform: scale(1.03);
    }

    .small {
      margin-top: 12px;
      color: #999;
      font-size: 13px;
    }
  </style>
</head>

<body>
  <div class="grid"></div>

  <main class="box" id="box">
    <div class="danger">⚠️</div>
    <h1>تم التهكير من venor</h1>
    <p class="sub">جاري تحليل الهاتف... لا تغلق الصفحة</p>

    <div class="terminal" id="terminal">
      <div class="line">Connecting to mobile device...</div>
      <div class="line">Connection established.</div>
    </div>

    <div class="progress-wrap">
      <div class="progress" id="progress"></div>
    </div>

    <div class="percent" id="percent">0%</div>

    <button onclick="showReveal()">إيقاف العملية</button>

    <div class="reveal" id="reveal">
     تعذر ايقاف العمليه<br>
      دي <span class="safe">تهكير الهاتف venor</span> تم.<br>
      يهبل تم تهكير،  صور،  كاميرا،  بيانات اتجمعت.
    </div>

    <p class="small">Prank page only — no real access, no data collection.</p>
  </main>

  <script>
    const terminal = document.getElementById("terminal");
    const progress = document.getElementById("progress");
    const percent = document.getElementById("percent");
    const box = document.getElementById("box");
    const reveal = document.getElementById("reveal");

    const lines = [
      "Reading device information...",
      "Checking front camera access...",
      "Checking gallery folders...",
      "Scanning WhatsApp cache...",
      "Finding saved passwords...",
      "Accessing secret files...",
      "Encrypting embarrassment folder...",
      "Sending report to venor server...",
      "Finalizing operation..."
    ];

    let i = 0;
    let value = 0;

    const interval = setInterval(() => {
      if (i < lines.length) {
        const div = document.createElement("div");
        div.className = "line";
        div.textContent = lines[i];
        terminal.appendChild(div);
        i++;
      }

      value += Math.floor(Math.random() * 12) + 7;
      if (value > 100) value = 100;

      progress.style.width = value + "%";
      percent.textContent = value + "%";

      if (value >= 100) {
        clearInterval(interval);
        box.classList.add("shake");

        setTimeout(() => {
          box.classList.remove("shake");
          showReveal();
        }, 1200);
      }
    }, 750);

    setTimeout(() => {
      showReveal();
    }, 8000);

    function showReveal() {
      reveal.style.display = "block";
      percent.textContent = "PRANK 😂";
      progress.style.width = "100%";
      box.classList.remove("shake");
    }
  </script>
</body>
</html>